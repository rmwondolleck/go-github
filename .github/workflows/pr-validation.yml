name: PR Validation

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  build:
    name: Build Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Download dependencies
        run: go mod download

      - name: Build
        run: go build -v ./...

      - name: Verify build artifacts
        run: |
          if [ -f "go-github" ]; then
            echo "Build successful: go-github binary created"
            ls -lh go-github
          else
            echo "Warning: Binary not found, but build completed"
          fi

  test:
    name: Test Validation
    needs: build
    uses: ./.github/workflows/test-reusable.yml
    with:
      go-version: '1.25'
